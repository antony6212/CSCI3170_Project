sudo su root; apt update; apt -y install mysql-server; service mysql start; mysql; CREATE USER 'userrr'@'%' IDENTIFIED BY 'pwww'; GRANT ALL PRIVILEGES ON *.* TO 'userrr'@'%' WITH GRANT OPTION; FLUSH PRIVILEGES; exit; cd src; javac Main.java;

// userrr is the username and pwww is the password for mysql, change it if you like
// to use mysql, type  mysql -u userrr -p

java -cp ./mysql-connector-j-8.0.32.jar:. Main
